```shell
cargo install --locked  gmt_dos-clients_fem --bin static_gain --features serde,clap
```

```shell
static_gain -i MC_M2_PZT_F -o MC_M2_PZT_D -f pzt_f2d.mat  
static_gain -i MC_M2_PZT_F -o MC_M2_PZT_D -f pzt_f2d.mat  
```
